[pytest]
# Daftar marker agar tidak muncul warning Unknown pytest.mark
markers =
    smoke: tes untuk skenario utama / critical path
    regression: tes untuk memastikan fitur lama tetap berjalan
    unit: tes untuk unit terkecil dari kode

# Aktifkan mode async otomatis untuk pytest
asyncio_mode = auto
